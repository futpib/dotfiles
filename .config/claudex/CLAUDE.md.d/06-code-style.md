# Code Style Guidelines

- When editing code, match the commenting style - add comments if the existing code has them, don't add comments if it doesn't
- README files are not always named `README.md` — they can be `readme.md`, `readme.tex`, or other variations. Check what the project actually uses before creating or editing one.